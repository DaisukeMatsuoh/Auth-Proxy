# ── auth-proxy Docker Compose設定用 ──────────────────────────────────
# これらの値はdocker-compose.ymlで使用され、auth-proxyコンテナに渡されます。
# 上流アプリが既に.envを使用している場合は、以下の値だけを追加してください。

# ホスト側ポート（Traefik/nginxから接続するポート）
AUTH_PROXY_HOST_PORT=8080

# コンテナ側ポート（auth-proxyがリッスンするポート）
AUTH_PROXY_LISTEN_PORT=8080
